# Keyboard Screen Studio 设计规范

版本：1.0  
适用范围：Windows 控制端界面、设备预览组件、后续新增页面与弹窗。

## 1. 设计原则

1. **克制、清晰、可预测**：界面用于设备配置，不用装饰性图标堆砌信息；主要操作依靠清晰文字、层级和状态表达。
2. **同类同形**：同一语义的控件必须使用同一模板、尺寸、状态和文字样式，页面不得自行复制一套近似样式。
3. **4 px 网格**：尺寸和间距以 4 px 为基础，只有 1 px 描边和锁定设备模型可以例外。
4. **内容优先**：说明文本最多两行；关键信息不得依赖浅色、低对比度或仅靠颜色识别。
5. **真实预览**：设备预览严格保持 142:428 画面比例、固件状态遮挡和物理框架关系，不为了页面排版拉伸。
6. **设置即生效**：不提供常驻“保存”或“推送”按钮；方案和设置变化自动保存、重新渲染并推送。连续输入和滑块操作使用 450 ms 防抖，最终值不得丢失。
7. **符合 Windows 习惯**：最小化、关闭、托盘驻留和恢复必须保持窗口原有大小与最大化状态；托盘必须提供打开、刷新并推送、退出三个基础操作。

## 2. 设计令牌

所有令牌集中在 `src/KeyboardScreen.App.Avalonia/Styles/DesignSystem.axaml`。页面不得新增语义重复的颜色、圆角或控件模板。

### 2.1 字体

- 应用 UI 唯一字体栈：Segoe UI Variable Text, Segoe UI, Microsoft YaHei UI。仅引用 Windows 系统字体，不在源码、安装包或便携版中附带字体文件。
- 窗口、导航、按钮、输入框、下拉菜单、提示和弹窗全部继承该字体。
- 字重：辅助说明、正文与控件使用 Regular 400，分组标题、页面标题、导航选中态和关键数字使用 SemiBold 600；禁止合成伪粗体。
- 字号：11（辅助标签）、12（说明）、13（控件）、15（卡片标题）、20（页面标题）、25（应用功能标题）。
- 唯一例外：字体选择页的“字体实时预览”和键盘画面渲染使用用户选择的字体。用户字体不得渗透到控制端其他 UI。
- **可编辑文字框不允许设置垂直内边距**：TextBox 与 PasswordBox 仅设置水平 Padding（上、下必须为 0），并使用 VerticalContentAlignment="Center"。WPF 编辑视口的行框度量与 TextBlock 不同，垂直 Padding 会压缩 PART_ContentHost，并可能把中文回退字体裁成横线。
- 页面不得覆盖输入框模板，也不得为单个输入框重新加入非零的上下 Padding。标准输入框高度仅允许 36px、40px、44px；使用紧凑高度时 Height 与 MinHeight 必须同时设置为相同值，避免公共 MinHeight=44 造成二次裁切。三种高度必须复用 DesignSystem.xaml 的公共模板。
- 修改输入框字体、字号、高度、内边距或 PART_ContentHost 后，必须运行无窗口 UI 烟雾测试；“北京”和“Ag09”的实际墨迹高度必须完整，不能只以编译通过作为验收。
- 固定格式字段必须在真实父级约束下按“完整内容 + 光标 + 两侧内边距”验证，不能给星号列子项设置超出可用列宽的 MinWidth。主题强调色行以 243px 最小内容宽度为基准：颜色圆 32px、间距 8px、按钮 88px，HEX 输入框获得 107px；UI 烟雾测试必须同时检查 7 个字符完整、尾部光标未裁切以及输入框与按钮不相交。

### 2.2 圆角

- `Radius.Window = 20`：主内容容器和模态窗口。
- `Radius.Card = 16`：信息卡片、预览侧栏。
- `Radius.Control = 12`：按钮、输入框、下拉框、导航项。
- `Radius.Small = 8`：小型标签、数值框。
- `Radius.Toggle = 11`：开关胶囊。
- 圆形状态点、色块和开关滑块使用完整圆形，不套通用圆角。
- 禁止用一个圆角值覆盖所有控件。

### 2.3 间距与尺寸

- 页面外边距：24。
- 内容栏间距：24。
- 卡片内边距：20。
- 卡片间距：16。
- 标题与说明：8。
- 表单字段间距：12。
- 标准控件高度：44；紧凑按钮：36；标题栏按钮：46 × 40。
- 可点击区域不得小于 36 × 36。

### 2.4 色彩

- 背景和表面只使用 `Surface.*` 语义画刷。
- 文字只使用 `Text.Primary`、`Text.Secondary`、`Text.Disabled`。
- 描边只使用 `Stroke.Default`、`Stroke.Strong`、`Stroke.Focus`。
- 控制端交互统一使用白色与中性灰色，不跟随键盘画面主题色。
- “主题强调色”只影响键盘显示画面；不得把用户选择的颜色扩散到控制端按钮、导航或焦点态。
- 错误、成功和警告必须同时包含文字，不允许只显示颜色。

## 3. 组件规范

### 3.1 按钮

- 按钮统一为白底深色文字与 1 px 中性灰描边，不使用蓝色填充。
- Secondary：白色/半透明表面、1 px 描边、深色文字。
- Hover、Pressed、Disabled、KeyboardFocus 必须由统一模板提供；Hover 与 Pressed 仅使用浅灰层级变化。
- 禁止系统虚线焦点框；键盘焦点使用中性灰描边。
- 按钮不因文字长度改变高度，不使用变形图标填充空白。

### 3.2 输入框与下拉框

- 高度统一为 44，圆角 12，1 px 描边。
- Hover 使用加强描边；Focus 使用 2 px 蓝色描边。
- 下拉面板与输入框使用相同圆角和字体，不允许回退到系统灰色直角模板。
- 路径和长文本必须截断，完整内容通过 ToolTip 或状态文本提供。

### 3.3 开关、滑块与滚动条

- 布尔设置使用 38 × 22 开关，不使用圆形勾选框冒充开关。
- 滑块轨道 4 px，滑块 16 px；数值显示使用独立小型数值框。
- 滚动条宽度 10 px，滑块 6 px、圆头、无系统箭头按钮。

### 3.4 卡片与导航

- 卡片统一使用 `SectionCard`；禁止在页面内直接写背景色和圆角。
- 一级导航只表达页面切换；选中态为浅灰表面和深色文字。
- 左侧方案列表只表达显示方案；选中态使用蓝色描边，不使用蓝底黑字。
- 左侧方案列表保持单层结构，顺序固定、名称简短；不加入分类标题、收藏、最近使用或搜索等额外层级。
- 标题、说明和控件必须按统一基线排列。

### 3.5 颜色选择器

- 禁止调用 WinForms 旧式 `ColorDialog`。
- 使用应用内统一弹窗，包含 HEX、RGB 滑块、色彩预览和常用色。
- 修改过程实时预览；只有“应用颜色”才提交，取消不得污染设置。

### 3.6 系统托盘与窗口状态

- “最小化到托盘”“关闭到托盘”“启动后最小化”分别配置，不互相隐式覆盖。
- 托盘双击恢复窗口，并恢复隐藏前的 Normal/Maximized 状态。
- 关闭到托盘启用时，标题栏关闭按钮只隐藏窗口；只有托盘“退出”结束进程。
- 托盘菜单固定包含：打开、刷新并推送、退出。
- 托盘图标和窗口隐藏不得停止采集、自动保存或定时推送。

## 4. 锁定组件：设备预览框架

`DevicePreviewControl` 是物理设备模型，不属于普通卡片，不得被页面设计随意改动。

### 4.1 不可变参数

- 控件外尺寸：154 × 440。
- 可视画面：142 × 428，比例和像素映射不可改变。
- 银色框宽：四边严格 6 px 等宽。
- 银色框颜色：`#D3D7DC`。
- 外圆角：26；内圆角：20；内外圆角同心，差值严格等于框宽。
- 画面不得额外增加黑色边框、阴影、描边或二次裁切。
- 固件遮挡：左侧圆形、右侧胶囊的位置和比例按 142 × 428 坐标绘制；一个贴左、一个贴右。
- 位图缩放使用 HighQuality；禁止页面缩放导致非整数框宽。

### 4.2 可以变化的内容

- 142 × 428 画面位图。
- 固件遮挡内部图标（后续拿到真实设备资源后）。
- 页面中预览卡片的说明文字。

### 4.3 修改门槛

任何涉及外尺寸、框宽、圆角、画面比例或遮挡坐标的改动，都必须：

1. 对照实机照片；
2. 在 100% 和 125% Windows 缩放下截图；
3. 检查四边等宽、四角连续、内外圆角同心；
4. 更新本规范的版本号和锁定参数。

### 4.4 AI 用量皮肤

- 主题 ID 固定为 `ai-quota`，作为独立 `IScreenTheme` 注册，不修改其他主题。
- 一次只渲染一个平台；平台名称位于能量条下方，不显示平台列表或切换控件。
- 主视觉为居中的竖向圆角能量条，填充从底部向上增长，高度严格对应剩余额度百分比。
- 平台名称下一行显示剩余额度；订阅制存在剩余次数时使用 `56% / 1次`，否则只显示 `56%`。
- 顶部继续遵守 52 px 固件安全区；辅助文字不得与左侧圆形和右侧胶囊指示器重叠。
- 数据模型同时容纳 API Key 的 token、费用、额度或请求数，以及订阅制百分比、剩余次数、重置周期和重置时间。
- 皮肤只负责读取通用快照并绘制；平台鉴权、API 请求与密钥存储必须由独立数据源适配器负责。

## 5. 可变与不可变边界

### 产品允许用户修改

- 键盘画面主题强调色。
- 键盘画面字体。
- 显示方案、图片、刷新间隔、自动化开关、设备地址和内容安全区。

### 产品开发可迭代但必须经过令牌

- 页面信息结构、说明文本、卡片组合、功能入口。
- 语义颜色和尺寸令牌（需要全局回归，不得单页覆盖）。

### 禁止随意修改

- 设备预览锁定参数。
- 142 × 428 与 512 KB JPEG 接口约束。
- 控制端唯一 UI 字体规则。
- 同类控件的模板和状态系统。
- 设置变更后的自动保存与自动推送管线。

## 6. 开发约束

- 新页面只能引用设计令牌和已命名样式。
- 除图像内容、状态色和锁定设备组件外，页面 XAML 不得出现新的 HEX 颜色。
- 不得在页面 XAML 新建 Button、TextBox、ComboBox、CheckBox、Slider、ScrollBar 模板。
- 需要新组件时先在 `DesignSystem.axaml` 定义，再在页面引用。
- 设备渲染字体与控制端 UI 字体必须使用不同的数据通道。

## 7. 交付检查表

- [ ] 100%、125%、150% 缩放下无裁切、错位和锯齿。
- [ ] 1080 × 680 最小窗口及最大化布局正常。
- [ ] 鼠标 Hover/Pressed、键盘 Focus、Disabled 状态一致。
- [ ] 没有系统灰色 ComboBox、系统箭头滚动条或虚线焦点框。
- [ ] 控制端除字体实时预览外全部继承 Windows 系统字体栈，发布物不包含 PingFang.ttc 或其他未获再分发授权的字体。
- [ ] 36px、40px、44px 输入框的“北京 / Ag09”在无焦点、悬停、聚焦状态下均完整垂直居中；所有输入框上下 Padding 为 0。
- [ ] 固定长度输入框在光标位于末尾时仍完整显示；HEX 颜色值的 7 个字符无裁切。
- [ ] 设备银框四边 6 px 等宽，四角连续，内外圆角同心。
- [ ] 键盘预览与实际 JPEG 使用同一帧。
- [ ] 图片方案导入后只推送一次，默认不定时推送。
- [ ] 方案切换与所有设置变更会自动保存并推送，连续操作只提交最终值。
- [ ] 最小化、关闭到托盘、托盘双击恢复、托盘退出和启动后最小化行为正确。
- [ ] 内置显示方案 ID 唯一，全部可渲染为 142×428 基线 JPEG 且不超过 512 KB。
- [ ] 绿色版字体目录和设置目录仍为应用目录下 `Fonts` 与 `Data`。
## 8. 桌面端动效规范

本节仅适用于 .NET WPF 控制端界面，不得进入 `KeyboardScreen.Core`、142×428 画面渲染、设备预览像素内容或 JPEG 输出管线。

- 设计依据：遵循 Apple Human Interface Guidelines 的连续性、直接操控、清晰反馈与克制原则；不照搬平台外观。
- 允许属性：优先且默认仅使用 `Opacity`、`RenderTransform.TranslateTransform` 与 `RenderTransform.ScaleTransform`。
- 禁止属性：不得动画 `Width`、`Height`、`Margin`、网格列宽、内边距或其他触发布局重算的属性。
- 时长：按压 90 ms，悬停 140 ms，释放与状态回稳 180 ms，页面或弹窗进入 220 ms，退出 130 ms。
- 曲线：进入和释放使用自然 `CubicEase/EaseOut`；按压与退出使用 `CubicEase/EaseInOut`。禁止弹簧、Bounce、Elastic、漂浮、呼吸和循环动画。
- 连续操作：所有代码动画使用 `HandoffBehavior.SnapshotAndReplace`，从元素当前视觉值接续到目标值，不允许排队或叠加 Storyboard。
- 直接操控：滑块、颜色选择器指针和滚动内容必须直接跟随输入，不给拖动位置增加延迟动画。
- 性能：不为动效新增大面积 Blur、DropShadow 或逐元素长列表动画；WebView、设备预览位图与大图内容不做持续动画。
- 辅助功能：当 Windows 关闭客户端动画时，界面必须立即到达最终状态，不强制播放动画。
- 高频交互覆盖：按钮、一级导航、方案列表、下拉菜单、开关、页面切换、上下文卡片、模态弹窗、最小化与恢复。
- 交互动效统一使用 XAML `Transitions` 与 `src/KeyboardScreen.App.Avalonia/Behaviors/` 下的行为类（如 EdgeFadeBehavior、SmoothScrollBehavior）；页面代码不得自行复制时长、缓动或动画管理逻辑。
- 新增界面：先复用全局控件样式；只有语义明确的页面/弹窗切换才调用 `InteractionMotion.Reveal` 或 `HideAsync`。

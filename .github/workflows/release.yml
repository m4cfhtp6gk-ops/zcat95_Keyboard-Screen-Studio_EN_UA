name: Build and publish release

on:
  push:
    tags:
      - "v*.*.*"
    branches:
      - "main"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    if: startsWith(github.ref, 'refs/tags/') || contains(github.event.head_commit.message, '[release v1.0.2]')
    name: Build Windows portable package
    runs-on: windows-latest
    env:
      RELEASE_TAG: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'v1.0.2' }}

    steps:
      - name: Check out source
        uses: actions/checkout@v6

      - name: Set up .NET 8
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "8.0.x"

      - name: Restore
        run: dotnet restore KeyboardScreenStudio.sln

      - name: Build
        run: dotnet build KeyboardScreenStudio.sln -c Release --no-restore

      - name: Run renderer smoke tests
        run: dotnet run --project tests/KeyboardScreen.SmokeTests/KeyboardScreen.SmokeTests.csproj -c Release --no-build

      - name: Run Avalonia UI smoke tests
        run: dotnet run --project tests/KeyboardScreen.AvaloniaSmokeTests/KeyboardScreen.AvaloniaSmokeTests.csproj -c Release --no-build

      - name: Build clean portable package
        shell: pwsh
        run: ./tools/Publish-Portable.ps1

      - name: Create release archive and checksum
        id: package
        shell: pwsh
        run: |
          $tag = $env:RELEASE_TAG
          $archiveName = "KeyboardScreenStudio-$tag-win-x64.zip"
          $checksumName = "$archiveName.sha256.txt"
          $archivePath = Join-Path $env:RUNNER_TEMP $archiveName
          $checksumPath = Join-Path $env:RUNNER_TEMP $checksumName

          Compress-Archive `
            -Path "artifacts/KeyboardScreenStudio-Portable/*" `
            -DestinationPath $archivePath `
            -CompressionLevel Optimal

          $hash = (Get-FileHash -LiteralPath $archivePath -Algorithm SHA256).Hash.ToLowerInvariant()
          "$hash  $archiveName" | Set-Content -LiteralPath $checksumPath -Encoding utf8

          "archive=$archivePath" >> $env:GITHUB_OUTPUT
          "checksum=$checksumPath" >> $env:GITHUB_OUTPUT

      - name: Create GitHub release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create $env:RELEASE_TAG `
            "${{ steps.package.outputs.archive }}" `
            "${{ steps.package.outputs.checksum }}" `
            --target $env:GITHUB_SHA `
            --title "$env:RELEASE_TAG" `
            --notes-file RELEASE_NOTES.md
